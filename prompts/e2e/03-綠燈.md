# TDD 綠燈階段：讓 E2E 測試通過

## 目標

在 TDD 紅燈階段已經寫好 E2E 測試並確認失敗。現在進入綠燈階段：

**寫最少的程式碼讓測試通過，實作後端 API，不斷 trial-and-error 直到所有測試變綠。**

---

## 程式架構規範

**重要**：本 prompt 的所有程式開發必須遵循 `@prompts/rules/程式架構規範.md` 的規則。

該文件定義了：
- 檔案組織結構（Controllers、Services、Repositories、Models、Schemas 的擺放位置）
- 分層架構職責（Controller、Service、Repository 的職責分配）
- 依賴注入原則
- 檔案創建順序

在實作過程中，請先閱讀該架構規範文件，確保所有程式碼符合規範。

---

## 核心原則

### 0. 測試驅動開發的鐵律

**必須透過執行自動化測試來驗證實作是否完成，絕不猜測。**

- ✅ **每完成一個步驟，立即執行測試**
- ✅ **無需詢問使用者，直接下達 `pytest` 指令**
- ✅ **根據測試結果決定下一步行動**
- ❌ **絕不假設測試會通過**
- ❌ **絕不詢問「測試通過了嗎？」**

**測試執行策略（重要）**：

1. **開發階段：先跑目標情境的特定測試**
   - 快速驗證當前實作
   - 縮短反饋循環
   - 專注於正在開發的功能

2. **完成階段：執行總回歸測試**
   - 確保沒有破壞既有功能
   - 驗證系統整體穩定性
   - 必須通過所有測試才算完成

**執行測試的時機與範圍**：
```
開發中（每個步驟後）：
→ pytest tests/e2e/test_create_order.py -v
  （只跑當前功能的測試，快速反饋）

→ pytest tests/e2e/test_create_order.py::Test訂單金額必須大於零::test_成功建立訂單 -v
  （只跑特定測試方法，更快速）

完成時（最終驗證）：
→ pytest tests/ -v
  （跑所有測試，確保沒有破壞既有功能）
```

**測試指令範例**：
```bash
# 1. 開發階段：執行特定測試檔案（快速迭代）
pytest tests/e2e/test_create_order.py -v

# 2. 開發階段：執行特定測試方法（最快）
pytest tests/e2e/test_create_order.py::Test訂單金額必須大於零::test_成功建立訂單 -v

# 3. 完成階段：執行所有測試（總回歸測試）
pytest tests/ -v
```

**重要提醒**：
- ✅ 開發時：頻繁執行特定測試（快速反饋）
- ✅ 完成前：必須執行 `pytest tests/ -v`（總回歸測試）
- ✅ 只有當**所有測試**都通過，才算完成綠燈階段
- ❌ 不要只跑特定測試就宣稱完成

### 1. 最小增量開發原則

只寫讓測試通過所需的最少程式碼，不要多做。

**範例概念**：
```python
# ❌ 做太多了（測試沒要求）
@router.post("/orders")
def create_order(request):
    validate_inventory()      # 沒測試
    send_email_notification() # 沒測試
    log_audit_trail()        # 沒測試
    return create_order_logic(request)

# ✅ 剛好夠（只實作測試要求的）
@router.post("/orders")
def create_order(request):
    return create_order_logic(request)
```

### 2. Trial-and-Error 流程

**核心流程**：測試 → 看錯誤 → 修正 → 再測試（循環直到通過）

```
開發循環（快速迭代）：
1. 執行特定測試 → pytest tests/e2e/test_xxx.py -v
2. 看錯誤訊息 → 理解失敗原因
3. 寫最少的程式碼修正這個錯誤
4. 再次執行特定測試 → pytest tests/e2e/test_xxx.py -v
5. 還有錯誤？回到步驟 2
6. 特定測試通過？進入完成驗證

完成驗證（回歸測試）：
7. 執行所有測試 → pytest tests/ -v
8. 所有測試通過？✅ 完成綠燈！
9. 有測試失敗？回到步驟 2，修復破壞的測試
```

**重要**：
- ✅ **開發時：頻繁執行特定測試**（快速反饋，節省時間）
- ✅ **完成前：必須執行總回歸測試**（確保沒有破壞既有功能）
- ✅ **每次修改後立即執行測試，不要累積多個變更**
- ✅ **直接執行 pytest 指令，不需要詢問使用者**
- ✅ **根據實際的測試輸出（HTTP 狀態碼、錯誤訊息）決定下一步**
- ❌ **不要一次寫完所有程式碼**
- ❌ **不要假設測試會通過**
- ❌ **不要只跑特定測試就宣稱完成**

**範例執行流程**：
```
開發階段（快速迭代）：
→ pytest tests/e2e/test_create_order.py -v
  FAILED: HTTP 404 Not Found
  
→ 建立 API endpoint
→ pytest tests/e2e/test_create_order.py -v
  FAILED: HTTP 500 Internal Server Error
  
→ 修正 Service 邏輯
→ pytest tests/e2e/test_create_order.py -v
  FAILED: HTTP 400 Bad Request (庫存不足)
  
→ 調整業務規則驗證
→ pytest tests/e2e/test_create_order.py -v
  PASSED ✅（特定測試通過）

完成驗證（回歸測試）：
→ pytest tests/ -v
  2 passed, 15 passed ✅（所有測試通過，真正完成！）
```

---

## 實作流程

遵循 `@prompts/rules/程式架構規範.md` 定義的架構，按照測試錯誤訊息逐步實作：

**基本流程**：
1. 執行測試 → `pytest tests/e2e/test_xxx.py -v`
2. 看錯誤訊息（HTTP 404? 500? 400?）
3. 根據錯誤補充最少的程式碼（schemas → services → controllers → 註冊路由）
4. 再次執行測試
5. 循環直到特定測試通過
6. 執行總回歸測試 → `pytest tests/ -v`

**重點**：
- ✅ 每個步驟後立即執行測試
- ✅ 根據測試輸出決定下一步
- ✅ 完成前必須執行 `pytest tests/ -v`
- ❌ 不要一次寫完所有程式碼

---

## 完成條件

綠燈階段完成的標準：

**開發階段**：
- ✅ 執行特定測試 `pytest tests/e2e/test_xxx.py -v`
- ✅ 確認目標功能測試通過

**完成驗證（必須）**：
- ✅ **執行總回歸測試 `pytest tests/ -v`**
- ✅ **所有測試通過（綠燈 🟢）**
- ✅ 沒有破壞既有功能
- ✅ 程式碼簡單直接

**驗證方式**：
```bash
# 開發階段：執行特定功能的測試（快速迭代）
pytest tests/e2e/test_create_order.py -v

# 完成驗證：執行所有測試（總回歸測試，必須）
pytest tests/ -v
```

**只有當執行 `pytest tests/ -v` 顯示所有測試 PASSED 時，才算完成綠燈階段。**

**不需要**：
- ❌ 程式碼優雅（留給重構階段）
- ❌ 效能優化（留給重構階段）
- ❌ 完整錯誤處理（測試沒要求就不做）
- ❌ 額外的 API 功能（測試沒要求就不做）
- ❌ 詢問使用者「測試通過了嗎？」（直接執行 pytest 確認）
- ❌ 只跑特定測試就宣稱完成（必須執行總回歸測試）

---

## 記住

1. **遵循架構規範** - 所有程式碼必須符合 `@prompts/rules/程式架構規範.md` 的規則
2. **測試驅動你** - 看測試錯誤決定下一步要實作什麼
3. **最小實作** - 只寫通過測試需要的程式碼
4. **Trial-and-Error** - 執行測試 → 看錯誤 → 修正 → 再執行，不斷循環
5. **自動執行測試** - 每次修改後立即執行測試，無需詢問使用者
6. **根據實際結果行動** - 依據 pytest 的輸出決定下一步

**測試執行的黃金原則**：
- ✅ **開發時：頻繁執行特定測試** `pytest tests/e2e/test_xxx.py -v`（快速迭代）
- ✅ **完成前：必須執行總回歸測試** `pytest tests/ -v`（確保穩定）
- ✅ **有疑問？執行測試**
- ✅ **修改完成？執行測試**
- ✅ **想確認進度？執行測試**
- ❌ **不要詢問「測試通過了嗎？」**
- ❌ **不要假設測試結果**
- ❌ **不要只跑特定測試就宣稱完成**

**完成判定標準**：
```
特定測試通過 ✅ → 功能開發完成
                ↓
         執行總回歸測試
                ↓
        pytest tests/ -v
                ↓
        所有測試通過 ✅✅✅
                ↓
         真正完成綠燈！
```

完成綠燈後，進入重構階段 ♻️

