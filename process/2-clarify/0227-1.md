2026/02/27

● 讀完了整份實作計畫書。以下是我認為需要澄清的問題：

  ---
  1. 授權不一致

  計畫書裡 LICENSE 寫 CC-BY-SA-4.0，但 repo 實際的 LICENSE 檔案是 CC0 1.0。程式碼授權要用哪一個？這會影響社群貢獻和 Convention Registry 的法律基礎。
  ==> 請明列這兩種的不同，會怎麼影響社群貢獻和 Convention Registry 的法律基礎?

  這是一個非常關鍵且常見的開源專案決策！授權條款（License）不僅是法律文件，更是專案對社群傳達的「價值觀與遊戲規則」。

首先要釐清一個基礎概念：Creative Commons (CC) 系列授權條款，官方強烈不建議用於「軟體程式碼」（因為 CC 沒有處理專利權以及源碼編譯等軟體特有問題）。因此，我們通常會將專案分為「程式碼 (Code)」與「資料/文本 (Data/Content)」來討論。

以下為您明列 CC0 1.0 與 CC-BY-SA-4.0 的核心差異，以及它們如何影響您的橋牌 DSL 專案、社群貢獻與 Convention Registry：

1. 核心差異明細

| 特性 | CC0 1.0 (對應程式碼的 MIT/Unlicense) | CC-BY-SA 4.0 (對應程式碼的 GPL 系) |
| --- | --- | --- |
| 定義 | 零版權 (公眾領域)：放棄所有著作權。 | 姓名標示-相同方式分享：保留著作權但開放使用。 |
| 使用限制 | 無限制。任何人皆可複製、修改、散布、商業使用。 | 需標示原作者 (BY)；若修改或衍生，必須以相同授權釋出 (SA)。 |
| 商用友善度 | 🌟🌟🌟🌟🌟 極高 | ⭐ 低 (具「傳染性」，商業公司通常避之唯恐不及) |
| 對衍生作要求 | 不要求開源修改後的心血。 | 強制開源 (Copyleft 特性)。 |

2. 對「社群貢獻」的影響
這涉及到您希望建立什麼樣的開源生態：

* 若選擇 CC0 (或 MIT)：
  - 優點：門檻極低，推廣阻力最小。學術界、商業橋牌平台（如 BBO 或是其他橋牌軟體公司）可以直接把您的 Parser 整合進他們的核心系統中，不用擔心法律問題。
  - 缺點 (社群反噬)：別人可以 fork 您的程式碼或註冊表，加上超棒的新功能然後閉源收費。他們沒有義務將修改回饋給您的主線 (Upstream)。
* 若選擇 CC-BY-SA (或 GPL/AGPL)：
  - 優點：保障「公共財」。任何開發者、廠商只要拿您的專案去修改或擴充，就**「必須」開源並貢獻回社群**。這能確保您的專案不會被微軟或某個大型橋牌平台給「白嫖」後私有化。
  - 缺點：許多商業專案的法務會禁止工程師引入 SA (ShareAlike) 或 GPL 授權的工具，這可能會嚴重限制您的解析器在業界被採用的廣度。

3. 對「Convention Registry」法律基礎的影響
在您的 Phase 5，會建立一個橋牌約定 (Bidding Conventions) 的 Registry。這是文字與資料庫 (Data)，非常適合使用 CC 授權。這兩種授權會造就完全不同的發展走向：

* 如果 Registry 採 CC0：
  - 您在打造橋牌界的 Wikipedia 早期。任何人都可以無痛把這些約定資料庫抓下來，放進自己的付費 App 裡當成賣點。這有利於把您的 bbDsl 格式推廣成世界絕對標準，但您和社群可能分不到商業利益。
* 如果 Registry 採 CC-BY-SA：
  - 您在打造橋牌界的 Wikipedia (維基百科正是使用 CC-BY-SA 授權)。如果某個社群大神貢獻了極其複雜的「精準制 (Precision) 變體」，任何平台想要內建這個變體，也必須把他們衍生的任何規則開源出來。這能築起極高的「資料護城河」，確保 Registry 的知識量永遠獨步全球且屬於全人類。

💡 最終架構建議 (雙軌授權 Dual Licensing)
在軟體工程與開源營運的實務上，面對這種「有工具又有資料內容」的專案，我們通常會採取**「程式碼與制度庫分離授權」**的策略：

1. 針對 Engine / CLI / Parser (程式碼部分)
   - 建議：MIT License (取代 CC0)。
   - 理由：工具的價值在於「被越多人用越好」。讓所有的橋牌打牌機器人、UI 編輯器都能毫無困難地嵌入您的解析引擎，這能幫助 bbDsl 成為業界標準 (De facto standard)。
2. 針對 Convention Registry (橋牌制度的 DSL 腳本資料庫)
   - 建議：CC-BY-SA-4.0 (或保留 CC0 如果您完全不在意他人拿去商用)。
   - 理由：語料庫是社群共創的心血。使用 CC-BY-SA 可以確保這個 Registry 持續增長，任何拿去優化制度的人都必須反哺社群。
您應該怎麼做： 請檢查您 Github Repo 目前的狀況。如果 repo 中混雜了 Python 程式碼和 BML/DSL 文本，建議在根目錄留一個明確的聲明，例如：

"The source code of this tool is licensed under the MIT License. All bridging bidding convention files (in the registry/ directory) are licensed under CC-BY-SA-4.0."

  2. 套件管理工具的選擇

  技術選型寫「uv / pyproject.toml」，但 uv 和 poetry 的 lockfile 格式和工作流差異很大。你要用哪一個？這決定了 CI/CD 設定和貢獻者的 onboarding 流程。
  ==> pyproject.toml 就表示用 poetry 嗎? 這怎麼決定了 CI/CD 設定和貢獻者的 onboarding 流程?

  這兩個是非常好的問題！我們逐一來釐清，這會幫助您未來在設定 Python 專案時更有方向感：

1. pyproject.toml 就表示用 Poetry 嗎？
不是的。

過去 Python 專案通常習慣用 setup.py 或 requirements.txt 來管理套件。但後來 Python 官方推動了標準化（PEP 518 和 PEP 621），將 pyproject.toml 制定為所有 Python 專案通用的設定檔標準。

現在，幾乎所有現代的 Python 工具共用這個檔案：

* 建置與依賴管理工具：Poetry, uv, Hatch, PDM, Flit 都讀取這個檔案（但各自會在裡面建立自己的專屬區塊 [tool.poetry] 或 [tool.uv]）。
* 程式碼品質工具：Ruff, Black, mypy, pytest 等也把設定寫在這個檔案裡的 [tool.ruff], [tool.pytest] 區塊。

所以看到 pyproject.toml，只代表這個專案是「現代化的 Python 專案」，並不直接等同於使用 Poetry。在您的計畫書中寫「uv / pyproject.toml」，意思是「使用 pyproject.toml 作為標準設定檔，並使用 uv 來作為核心的套件與環境管理工具」。

2. 這怎麼決定了 CI/CD 設定和貢獻者的 onboarding（報到/上手）流程？
當您決定要用 uv 或是 poetry 時，會產生截然不同的檔案結構與指令，這會直接影響別人怎麼參與你的專案，以及自動化腳本怎麼寫：

A. 對「貢獻者 Onboarding (新手上手流程)」的影響
貢獻者 clone 您的 repo 後，第一件事就是把環境架起來。

* 如果用 Poetry：
  * 貢獻者需要先安裝 Python： 他們得自己想辦法在電腦上裝好 Python (例如用 pyenv)。
  * 上手指令：貢獻者需要先按官方文件安裝 Poetry，然後執行 poetry install。Poetry 會自動建立虛擬環境 (.venv) 並安裝 poetry.lock 裡的套件。
  * 體驗：Poetry 生態系很成熟，指令直覺（poetry add、poetry run），但解析龐大依賴關係時有時會有點慢。
* 如果用 uv (推薦，這也是您原本計畫書的方向)：
  * 貢獻者不需要先裝 Python： uv 本身連 Python 版本都能幫你抓！貢獻者只要裝了 uv（一個用 Rust 寫的極速工具），執行指令，uv 會自動幫你下載正確版本的 Python。
  * 上手指令：貢獻者執行 uv sync。
  * 體驗：速度快到不講武德（通常比 Poetry 快 10-100 倍）。對於新手來說，不用處理「我的 Python 版本不對」的問題，是很棒的體驗。不過 uv 的專案管理功能（uv.lock 等）是 2024 下半年才剛推出的新功能，社群文章相對 Poetry 少一點點。

B. 對「CI/CD 設定 (如 Github Actions)」的影響
在做自動化測試或打包佈署時，CI 系統需要知道用什麼工具來還原環境。

* 如果用 Poetry：
  * GitHub Actions 裡需要先 actions/setup-python 安裝 Python，再安裝 Poetry 工具，利用 poetry.lock 來快取依賴。
  * 指令：poetry run pytest (跑測試)。
* 如果用 uv：
  * GitHub Actions 直接使用 astral-sh/setup-uv 這個 action。因為 uv 速度極快，甚至連依賴快取 (caching) 的設定有時都可以省了，因為它重新安裝可能只需要不到一秒鐘。
  * 指令：uv run pytest。
  * 您在 repo 中會提交一個叫做 uv.lock 的檔案（就如同 Node.js 的 package-lock.json），來確保 CI 上的套件版本和您本機一模一樣。

總結建議
以您原本計畫書的方向，建議 明確宣示採用 uv（搭配 pyproject.toml 與 uv.lock）。

為什麼？ 因為 uv 目前在 Python 社群是當紅炸子雞（由開發 Ruff 的同一個團隊 Astral 製作）。它統一了 pip, venv, pyenv, pip-tools 甚至部分 poetry 的功能。這能讓未來的貢獻者（或您自己在其他機器上）只要一行 uv sync，就能在幾秒內瞬間把一模一樣的開發環境架好，開發體驗非常絲滑！

  3. Phase 1 驗證規則數量矛盾

  - 總覽圖寫「驗證器 (8 規則)」
  - Sprint 1.2 任務清單列了 val-002, 004, 005, 006, 007, 008, 011, 012 = 8 條
  - 但 Phase 1 完成標準又說「valid/ 全部 PASS，invalid/ 全部偵測到預期的錯誤」

  val-001 (hcp-coverage) 和 val-003 (response-complete) 被推到 Phase 2，那 Phase 1  的精準制範例驗證時碰到這兩條怎麼處理？直接跳過、還是先產出 warning？
  ==> 直接跳過、還是先產出 warning 分別會有什麼影響?

這是一個非常務實的架構設計考量。

在驗證器（Validator）的設計中，val-001 (hcp-coverage 點力覆蓋率) 和 val-003 (response-complete 應叫完備性) 屬於 「語意與邏輯完備性 (Semantic/Logical Completeness)」 的深度檢查。它們比單純檢查「格式有沒有寫錯」要困難得多。

如果在 Phase 1 碰到這兩條規則，以下是兩種處理方式的具體影響與優缺點：

### 選項 A：直接跳過 (Silently Skip / Ignore)
也就是在程式碼中完全不註冊、不執行這兩條規則，當作它們還不存在。

* 對開發的影響 (優點)：Phase 1 的開發最乾淨俐落。您可以百分之百專注在基礎語法（Syntax）、不合法的叫品、重複條件等最基本的解析工作，不用去碰樹狀結構遍歷或點力區間交集的複雜演算法。
* 對範例與測試的影響 (缺點 - 虛假的安全感)：您在 Phase 1 寫的「精準制範例檔 (valid/precision.yaml)」，會順利得到 ✅ All validations passed.。但實際上，這個檔案可能存在嚴重的邏輯漏洞（例如漏規定了 15-17 點的開叫，或漏了對手插叫後的處理）。當您推進到 Phase 2 且實作了這兩條規則時，原本 PASS 的範例檔會突然吐出一大堆 Error，導致需要回頭大幅修改 Phase 1 寫好的 YAML 檔。

### 選項 B：先產出 Warning (Stub & Warn)
Phase 1 就定義好這兩條規則的 ID，但不進行真正的運算檢查，僅僅是拋出一個寫死的 Warning（警告），不把這視為會導致中斷的 Error。

* 對架構的影響 (優點 - 建立分級機制)：編譯器或驗證器早晚都需要區分 Error (致命語法錯誤，無法繼續) 與 Warning (不好的習慣或潛在的漏洞，但仍可編譯)。這樣做可以在 Phase 1 就強迫您把「錯誤分級機制」建立起來。
* 對使用者/測試的影響 (優點 - 管理期望)：跑 Phase 1 的驗證時，輸出會像是這樣：

```text
> bbDsl validate precision.yaml
[OK] val-002: Syntax is correct.
[OK] val-005: No duplicate condition branches.
[WARN] val-001 (WIP): HCP coverage check is skipped in Phase 1.
[WARN] val-003 (WIP): Response completeness check is skipped in Phase 1.
Result: ✅ Passed with 2 Warnings.
```

這樣能時刻提醒開發者「這份檔案格式對了，但邏輯不保證無死角」。
* 對開發的影響 (缺點)：會讓終端機輸出增加一些「雜訊」，而且您需要在 Phase 1 就設計好一套能收集、彙整並漂亮印出 Warning 的輸出格式。

---

💡 總結與建議
強烈建議採用 選項 B (先產出 Warning) 的「佔位符 (Stub)」策略。

理由：

1. 不會阻擋 CI/CD：只要定義「只有 Error 會讓腳本回傳 exit code 1 (失敗)，Warning 不會」，那麼您 Phase 1 的完成標準「valid/ 全部 PASS」依然能順利達成。
2. 避免「技術債驚喜」：當別人看到您的專案，就不會誤以為這個 Parser 已經強大到可以完美抓出所有的橋牌邏輯漏洞，清楚界定了當前版本的邊界。
3. 為 Phase 2 鋪路：等到 Phase 2 開始時，您只需要把原本回傳 Warning 的假函式，換成真正執行點力區間計算的演算法即可，架構不用大改。



4. _unresolved 和 _bml_original 的定位
`
  Pydantic 模型裡 _unresolved 用底線開頭（Python 的 private convention），但 Pydantic v2 對 _ 前綴的 field
  有特殊行為（不會在 serialization 時輸出）。這些欄位是：
  - 只在匯入流程中暫存，最終不寫入 YAML？
  - 還是會持久存入 .bbdsl.yaml 給使用者手動補完？

  如果是後者，欄位名稱不應該用底線前綴。
  ==> 請分析二者的利弊得失，給我建議

  這是一個關於「轉檔器/編譯器體驗 (Developer Experience)」的經典工程問題。針對處理不規則來源資料（如 BML），這兩個方向代表了截然不同的設計哲學。

以下為您分析這兩條路線的利弊，並給出強烈建議。

### 路線一：作為暫存狀態，不寫入 YAML（也就是保持 _ 前綴）
當 BML 匯入器遇到看不懂的句子時，在記憶體中標記為 _unresolved，但在最終輸出 .bbdsl.yaml 檔案時將其丟棄（或只在終端機印出警告）。

* 優點 (Pros)：
    * Schema 絕對純淨：您的 bbDsl 規範非常乾淨，不會染上「轉檔過程的過渡產物」。
    * 驗證邏輯簡單：Validator 不用去處理「欄位存在但內容是未知」的邊界情況。YAML 只要產得出來，結構就一定是 100% 正確的。
* 缺點 (Cons)：
    * 可怕的「靜默資料遺失 (Silent Data Loss)」：使用者匯入了一份 500 行的 BML 檔，結果輸出的 YAML 只有 450 行。那 50 行包含重要打牌約定的資料憑空消失了，使用者很難查覺到底是哪幾條沒被解析出來。

### 路線二：作為持久化欄位，寫入 YAML（必須移除 _ 前綴，例如改成 unresolved 和 bml_original）
當解析器遇到看不懂的 BML 句子時，依然照樣產生一個 YAML 節點，但裡面不放結構化資料，而是把原始字串塞進 bml_original 並標記為 unresolved: true。

* 優點 (Pros)：
    * 零資料遺失 (Zero Data Loss)：這對轉檔工具是最高指導原則。使用者絕對不想重新翻找原始 BML 檔。
    * 極佳的遷移體驗：使用者可以直接打開匯出後的 .bbdsl.yaml，用編輯器全域搜尋 unresolved:，然後看著旁邊完整的 bml_original 提示，逐一用手動方式補上正確的 DSL 語法。這比起看着終端機的一閃而過的錯誤訊息來得實用太多了。
* 缺點 (Cons)：
    * 污染了標準 Schema：您的正式 Pydantic 模型裡，必須容忍 unresolved 這種「不完美」的欄位存在。
    * Validator 必須跟著改：驗證器在檢查檔案時，必須特別寫一段邏輯：遇到 unresolved: true 的節點時，要拋出特定的編譯錯誤（例如：Error: Found unresolved BML rule at line 45. Please fix it manually.）。

###💡 最終建議與架構解法
強烈建議採用「路線二」（持久化寫入 YAML）。

橋牌的 BML 缺乏嚴格規範（有時甚至夾雜人類自然語言註解），自動解析器絕對不可能達到 100% 成功率。如果採取路線一，這個匯入工具的實用性會大打折扣。

但在 Pydantic 架構上，為了不讓「過渡期欄位」弄髒您乾淨的 DSL 標準，建議採用以下**「繼承/多型分層」**的解法：

具體實作建議
不要在標準核心的 CallNode 裡面放這兩個欄位，這會混淆標準格式。您可以把「未解析節點」做成一種特定的「錯誤類型節點」：

1. 修改 Pydantic 結構 (移除底線，變成合法 Node)

```python
from pydantic import BaseModel, Field
from typing import Union, List
# 1. 您原本乾淨的標準節點
class CallNode(BaseModel):
    call: str
    condition: str | None = None
    meaning: str | None = None
    next: List["Node"] = []
# 2. 新增一種專門用來接住匯入失敗的「未解析節點」
class UnresolvedNode(BaseModel):
    is_unresolved: bool = Field(default=True, const=True) # 標記
    bml_original: str  # 留下證據
    reason: str        # 為什麼解析失敗（例：不明的特殊符號）
# 3. 樹狀結構允許這兩種節點混居
Node = Union[CallNode, UnresolvedNode]
```

2. 這個設計帶來的完美工作流：

* 匯入時 (BML -> YAML)：解析器盡力而為。懂的變成 CallNode，不懂的退化成 UnresolvedNode，一起寫入 YAML。
* 使用者手動修復 (Manual Fix)：使用者打開 YAML，看到：

```yaml
- call: 1S
  next:
    - is_unresolved: true
      bml_original: "2C* (Ask for specific aces, weird convention)"
      reason: "Syntax Error: Unknown modifier '*'"
```

使用者就可以看著 bml_original 手動把這個區塊修掉，改成合乎 bbDsl 規範的寫法。
* 驗證與編譯時 (YAML -> JSON/AI Prompt)：您的 Validator 只要偵測到 Pydantic 實例是 UnresolvedNode，就直接噴發 CompilerError 阻止編譯，強迫使用者在編譯打包前一定要把所有 unresolved 修完。
這個作法既保護了您核心 Schema 的純潔性，又提供了最穩健、最不怕遺失資料的遷移體驗！

  5. OpponentPattern 的 matches() 方法

  模型裡放了 def matches(self, opponent_action: dict) -> bool 但標註 Phase 2 實作。Pydantic BaseModel
  裡放業務邏輯方法是有意為之的設計，還是打算後續把匹配邏輯移到 core/ 的獨立 service？
  ==> 這題的建議做法?

這是一個非常深入核心軟體工程設計（也就是 DDD 架構 - 領域驅動設計）的問題。

我們來看看您提的問題：

OpponentPattern 模型裡面放了 def matches(self, opponent_action: dict) -> bool。 Pydantic BaseModel 裡面放業務邏輯方法是有意為之，還是打算後續移到獨立 service？

在系統架構設計上，這兩種做法分別代表了 「Rich Domain Model (充血模型)」 與 「Anemic Domain Model (貧血模型) + Service」 的路線。

以下是針對這個問題的分析與強烈建議：

###選項 A：留在 Pydantic 內（Rich Domain Model 充血模型）
這是您計畫書目前寫的狀態。資料模型 (BaseModel) 不只儲存資料，還擁有操作這些資料的行為（方法）。

* 優點 (Pros)：
    * 高內聚 (High Cohesion)：關於「什麼是對手模式」以及「如何判斷一個行為符合該模式」，這些邏輯被緊緊綁在同一個類別裡。不管在哪裡呼叫，都不用再引入其他的輔助模組。
    * 直覺的物件導向：呼叫起來像自然語言 pattern.matches(action)。
* 缺點 (Cons)：
    * 違反單一職責 (SRP)：Pydantic 的主要責任是「資料驗證與解析」。如果在裡面塞滿複雜的遞迴比對邏輯、正規表達式與演算法，這個類別會變得很雜亂。
    * 耦合過重：如果要比對的過程需要存取其他的依賴項（例如：需要查閱 SUIT_META 或 Definitions 來知道 majors 代表什麼），放在模型內部就很難注入這些外部設定。

### 選項 B：移到獨立的 Service（Anemic Domain Model 貧血模型）
資料模型（Pydantic）只負責定義資料的形狀與驗證語法，而所有的業務邏輯（如 matches）都被抽離到 core/ 下的獨立函數或類別（例如 PatternMatcher）。

* 優點 (Pros)：
    * 職責分離 (Separation of Concerns)：models/context.py 只管資料乾不乾淨，core/matcher.py 只管演算法怎麼跑。這對測試非常友善。
    * 支援複雜上下文：如果比對時需要知道「現在是打什麼身價」或是「前面的人叫了什麼」，獨立的 Service 可以很容易地把整個 Context 或是 Definitions 物件當作參數傳進去。
* 缺點 (Cons)：
    * 呼叫稍微囉嗦：語法變成 PatternMatcher.matches(pattern, action, context)。

### 💡 最終建議：採用 選項 B (移到獨立 Service)
在您的橋牌 DSL 這個特定情境中，把邏輯從 Pydantic 抽離出來是絕對正確的選擇。

理由如下：

1. 比對邏輯太複雜，且具遞迴性 您在計畫書裡定義的 OpponentPattern 支援 any_of, all_of, not_ 等組合模式。如果把這些邏輯寫在 BaseModel 裡，程式碼會變得很肥大難讀。
2. 依賴外部定義 (Dependencies) 當 pattern.suit == "major" 時，matches 方法需要去查閱系統定義的 SUIT_GROUPS 才能知道這包含紅心和黑桃。如果寫在 BaseModel 裡，您很難乾淨地把 SUIT_GROUPS 傳遞進去給每一個節點。
3. Phase 4 的模擬引擎需求 到了 Phase 4 (AI 與模擬引擎)，您不只要判斷「模式是否匹配」，您可能還要「生成符合模式的叫品」。這時候如果邏輯都綁死在 Pydantic 裡，會造成嚴重的架構瓶頸。

### 具體修改建議
讓模型變「貧血 (純資料)」： 在 models/context.py 裡，純粹定義資料結構，把 def matches() 砍掉。

```python
class OpponentPattern(BaseModel):
    simple: Optional[str] = None
    bid: Optional[str] = None
    # ... (保留所有欄位) ...
    # 刪除 matches 方法
```

在 core 目錄下建立專屬的引擎： 新增一個 core/matcher.py 或 core/evaluator.py：

```python
# bbdsl/core/matcher.py
from bbdsl.models.context import OpponentPattern
class PatternMatcher:
    def __init__(self, definitions: dict):
        """可以把全域的名詞定義注入進來"""
        self.definitions = definitions
    def matches(self, pattern: OpponentPattern, action: dict) -> bool:
        # 實作比對邏輯...
        if pattern.simple:
            return self._match_simple(pattern.simple, action)
        # ...遞迴處理 any_of, all_of
```

這個改動能確保您的 models 目錄永遠輕量、乾淨，只做「讀取 YAML 和驗證格式」這件事，這是構建健壯編譯器的黃金法則。



  6. BML 匯入的範圍定義

  BML 本身沒有正式規格（只有 convention），不同作者的 BML 寫法差異很大。計畫書目標「HCP/花色長度正確提取率 > 80%」——這個
   80% 是基於哪些 BML 樣本測量的？有沒有預定的測試語料庫？

  這是一個切中要害的 QA 測試策略問題！

其實在目前的開源橋牌界，沒有一個官方標準的 BML 測試語料庫（Corpus）。計畫書中提到的「正確提取率 > 80%」是一個「工程啟發式（Heuristic）的目標」，也就是說，這 80% 是需要我們自己建立 Benchmark (基準測試) 來測量的。

為了讓這個 80% 有科學依據，且確保您的 BML 匯入器真的實用，我建議採取以下策略來定義這個範圍和測試語料庫：

1. 建立「黃金標準語料庫 (Golden Corpus)」
在 Phase 1，您不需要去網路上隨便找幾百份格式千奇百怪的 BML 來為難自己。您只需要在 tests/fixtures/bml_samples/ 裡面放入 3~5 份最具代表性的 BML 檔案 即可。

推薦的樣本來源：

* 原版 BML 官方範例：到 BML 的發明者 (biefnes 或 kalkin) 的 GitHub repo 中，直接下載他們提供的 SAYC.bml 和 Precision.bml。這是最正統的寫法。
* 常見的社群變體：去 GitHub 搜尋 .bml 檔案，挑選 1~2 份星數較高、排版比較整齊的 2/1 GF (二蓋一) 制度檔。
* 您自己手寫的測試檔：為了測試極限，寫一份包含各種寫法（如 12-14 HCP、12--14、12+、5+H、5!h）的假檔案。

2. 什麼是「正確提取率 > 80%」？（如何測量）
這個指標的定義不該是「80% 的檔案能匯入」，而是「對於語料庫中的每一條規則，有多大比例的文字描述被成功轉化為結構化屬性」。

具體的測量與測試方法如下：

* 人工標註 (Ground Truth)：您先看著語料庫裡的某 100 條 BML 叫品，人工算一下裡面其實包含了 40 個 HCP 條件和 30 個花色長度條件。
* 程式測試：跑您的 bml_importer.py，看看它用 Regular Expression (正規表達式) 抓出了幾個。
* 達標標準：如果您的程式成功抓出 32 個 HCP (32/40 = 80%) 和 24 個花色長度 (24/30 = 80%)，剩下的它看不懂、被放到 _unresolved (或 unresolved=True) 裡面去，這樣就算是完成了 Phase 1 的目標！

3. 給您的實務架構建議
在實作 BML 匯入器時，千萬不要寫一個巨大無比、想要解析所有自然語言的 NLP 引擎。

請採用 「管線過濾策略 (Pipeline & Heuristics)」：

* 先寫 3 條最基本的 Regex（抓 XX-YY 點力、抓 N+! 花色長度、抓 BAL/GF/INV 等關鍵字）。
* 用這些 Regex 去跑您的語料庫，看能抓出多少。
* 如果只有 50%，那就再加 2 條 Regex 去處理另一種常見寫法。
* 一旦達到 80%，馬上停手！

剩下的 20%（比如作者用很口語的英文寫 "a bad 12 to a good 14 with a singleton club"）是 Regex 處理不了的。這正是為什麼我們前面討論要保留 unresolved 和 bml_original 的原因。剩下的 20% 就交給使用者匯入後去人工手動修復（或留到 Phase 4 讓 AI 去讀）。

這樣可以確保您的 Phase 1 不會陷入「永遠寫不完 BML 解析器」的泥淖中。

 

  7. Phase 5 技術棧跳躍

  Phase 1-4 全部是 Python CLI 工具，Phase 5 突然出現：
  - FastAPI + PostgreSQL（後端）
  - React + Monaco Editor（前端）
  - WebSocket 即時通訊
  - OAuth 使用者系統

  這等於是另起一個全端 web 專案。有考慮過 Phase 5 獨立成另一個 repo 嗎？或者你打算 monorepo 處理？

  8. 單人開發的優先級取捨

  風險表裡已經指出「單人開發瓶頸」機率高。32 週的計畫涵蓋 CLI + 匯入匯出 + 視覺化 + AI 模擬 +
  社群平台，範圍非常廣。如果時間不夠，Phase 3/4/5 之間的優先順序是什麼？例如：AI 整合 vs
  視覺化，哪個對你的目標使用者更重要？

  ---
  這些不影響 Phase 1 開工，但越早定下來，後面的架構決策越穩。你想先回答哪些？

  ---

  讀完了整份實作計畫書。以下是我認為需要澄清的問題：

  ---
  1. 授權不一致

  計畫書裡 LICENSE 寫 CC-BY-SA-4.0，但 repo 實際的 LICENSE 檔案是 CC0 1.0。程式碼授權要用哪一個？這會影響社群貢獻和 Convention Registry 的法律基礎。
  ==> 雙軌授權 Dual Licensing
  程式碼採用 MIT License (取代 CC0)。
  Convention Registry 採用 CC-BY-SA-4.0。
  repo 的 LICENSE 檔案改成 CC-BY-SA-4.0 並在根目錄留一個明確的聲明:
  "The source code of this tool is licensed under the MIT License. All bridging bidding convention files (in the registry/ directory) are licensed under CC-BY-SA-4.0."

  2. 套件管理工具的選擇

  技術選型寫「uv / pyproject.toml」，但 uv 和 poetry 的 lockfile 格式和工作流差異很大。你要用哪一個？這決定了 CI/CD   設定和貢獻者的 onboarding 流程。
  ==> uv（搭配 pyproject.toml 與 uv.lock）

  3. Phase 1 驗證規則數量矛盾

  - 總覽圖寫「驗證器 (8 規則)」
  - Sprint 1.2 任務清單列了 val-002, 004, 005, 006, 007, 008, 011, 012 = 8 條
  - 但 Phase 1 完成標準又說「valid/ 全部 PASS，invalid/ 全部偵測到預期的錯誤」

  val-001 (hcp-coverage) 和 val-003 (response-complete) 被推到 Phase 2，那 Phase 1
  的精準制範例驗證時碰到這兩條怎麼處理？直接跳過、還是先產出 warning？
  ==> 採用 選項 B (先產出 Warning) 的「佔位符 (Stub)」策略

  4. _unresolved 和 _bml_original 的定位

  Pydantic 模型裡 _unresolved 用底線開頭（Python 的 private convention），但 Pydantic v2 對 _ 前綴的 field
  有特殊行為（不會在 serialization 時輸出）。這些欄位是：
  - 只在匯入流程中暫存，最終不寫入 YAML？
  - 還是會持久存入 .bbdsl.yaml 給使用者手動補完？

  如果是後者，欄位名稱不應該用底線前綴。
  ==> 路線二：作為持久化欄位，寫入 YAML（必須移除 _ 前綴，例如改成 unresolved 和 bml_original）

    橋牌的 BML 缺乏嚴格規範（有時甚至夾雜人類自然語言註解），自動解析器絕對不可能達到 100% 成功率。如果採取路線一，這個匯入工具的實用性會大打折扣。

    但在 Pydantic 架構上，為了不讓「過渡期欄位」弄髒您乾淨的 DSL 標準，建議採用以下**「繼承/多型分層」**的解法：

    具體實作建議
    不要在標準核心的 CallNode 裡面放這兩個欄位，這會混淆標準格式。您可以把「未解析節點」做成一種特定的「錯誤類型節點」：

    1. 修改 Pydantic 結構 (移除底線，變成合法 Node)

    ```python
    from pydantic import BaseModel, Field
    from typing import Union, List
    # 1. 您原本乾淨的標準節點
    class CallNode(BaseModel):
        call: str
        condition: str | None = None
        meaning: str | None = None
        next: List["Node"] = []
    # 2. 新增一種專門用來接住匯入失敗的「未解析節點」
    class UnresolvedNode(BaseModel):
        is_unresolved: bool = Field(default=True, const=True) # 標記
        bml_original: str  # 留下證據
        reason: str        # 為什麼解析失敗（例：不明的特殊符號）
    # 3. 樹狀結構允許這兩種節點混居
    Node = Union[CallNode, UnresolvedNode]
    ```

    2. 這個設計帶來的完美工作流：

    * 匯入時 (BML -> YAML)：解析器盡力而為。懂的變成 CallNode，不懂的退化成 UnresolvedNode，一起寫入 YAML。
    * 使用者手動修復 (Manual Fix)：使用者打開 YAML，看到：

    ```yaml
    - call: 1S
      next:
        - is_unresolved: true
          bml_original: "2C* (Ask for specific aces, weird convention)"
          reason: "Syntax Error: Unknown modifier '*'"
    ```

    使用者就可以看著 bml_original 手動把這個區塊修掉，改成合乎 bbDsl 規範的寫法。
    * 驗證與編譯時 (YAML -> JSON/AI Prompt)：您的 Validator 只要偵測到 Pydantic 實例是 UnresolvedNode，就直接噴發 CompilerError 阻止編譯，強迫使用者在編譯打包前一定要把所有 unresolved 修完。
    這個作法既保護了您核心 Schema 的純潔性，又提供了最穩健、最不怕遺失資料的遷移體驗！


  5. OpponentPattern 的 matches() 方法

  模型裡放了 def matches(self, opponent_action: dict) -> bool 但標註 Phase 2 實作。Pydantic BaseModel
  裡放業務邏輯方法是有意為之的設計，還是打算後續把匹配邏輯移到 core/ 的獨立 service？
  ==> 後續把匹配邏輯移到 core/ 的獨立 service

  6. BML 匯入的範圍定義`

  BML 本身沒有正式規格（只有 convention），不同作者的 BML 寫法差異很大。計畫書目標「HCP/花色長度正確提取率 > 80%」——這個 80% 是基於哪些 BML 樣本測量的？有沒有預定的測試語料庫？
  ==> 預計使用 10 份公開的 BML 範例（包含不同作者和風格）進行測試，目標是 HCP 提取率 > 80%、花色長度提取率 > 80%。

  7. Phase 5 技術棧跳躍

  Phase 1-4 全部是 Python CLI 工具，Phase 5 突然出現：
  - FastAPI + PostgreSQL（後端）
  - React + Monaco Editor（前端）
  - WebSocket 即時通訊
  - OAuth 使用者系統

  這等於是另起一個全端 web 專案。有考慮過 Phase 5 獨立成另一個 repo 嗎？或者你打算 monorepo 處理？
  ==> Phase 5 獨立成另一個 repo

  8. 單人開發的優先級取捨

  風險表裡已經指出「單人開發瓶頸」機率高。32 週的計畫涵蓋 CLI + 匯入匯出 + 視覺化 + AI 模擬 +   社群平台，範圍非常廣。如果時間不夠，Phase 3/4/5 之間的優先順序是什麼？例如：AI 整合 vs  視覺化，哪個對你的目標使用者更重要？
  ==> Phase 3 (視覺化) > Phase 4 (AI 整合) > Phase 5 (社群平台)

---

三個任務全部完成。以下是變更摘要：

  Task 1: 授權檔案

  - LICENSE — 改為 MIT License
  - LICENSE-CC-BY-SA-4.0 — 新增 Convention 檔案授權
  - LICENSING.md — 新增雙軌授權聲明（中英雙語）

  Task 2: BBDSL_IMPLEMENTATION-PLAN.md

  - 新增 ADR 區塊（8 項架構決策記錄），置於文件最前方作為權威參考
  - 更新專案結構：授權檔案、新增 opponent_matcher.py
  - 更新技術選型：uv + pyproject.toml + uv.lock
  - 更新 Pydantic 模型範例：移除 _unresolved/_bml_original，改為 ADR-4 的 UnresolvedNode 分層設計
  - 更新 OpponentPattern：移除 matches() 方法（ADR-5）
  - 更新 BML 匯入器演算法：回傳 CallNode | UnresolvedNode
  - Phase 5 加上獨立 repo 註記（ADR-7）
  - 風險表更新 BML 相關緩解策略

  Task 3: CLAUDE.md + README.md

  - 套件管理改為 uv
  - 授權 badge 和說明改為雙軌授權
  - 新增 ADR 快速參考區塊
  - 套件結構加入 UnresolvedNode 和 opponent_matcher 說明

  ---

  ● Sprint 1.1 全部完成。總結：

  ---
  Sprint 1.1 交付物清單

  ┌───────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────┐
  │     驗收項目      │                                            狀態                                             │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │ pyproject.toml +  │ uv sync 成功                                                                                │
  │ uv 初始化         │                                                                                             │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │                   │ common.py (4 enums + Range/Author/Completeness), bid.py                                     │
  │ Pydantic 模型 — 5 │ (HandConstraint/BidMeaning/BidNode), convention.py (Convention + ID regex), context.py      │
  │  個模組           │ (OpponentPattern/Context/ContextOverride), system.py                                        │
  │                   │ (Definitions/SystemMetadata/BBDSLDocument)                                                  │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │ YAML Loader       │ load_document() + print_summary() + generate_json_schema()                                  │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │ CLI 骨架          │ bbdsl load + bbdsl schema 可執行                                                            │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 精準制範例        │ examples/precision.bbdsl.yaml — 9 個開叫、2 個 Convention、3 個牌型、載入無錯誤             │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │ JSON Schema       │ schema/bbdsl-v0.3-generated.json 已產出                                                     │
  │ 自動產生          │                                                                                             │
  ├───────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 單元測試          │ 63 tests passed, 87% coverage                                                               │
  └───────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────┘

---

● Sprint 1.2 完成總結

  101 tests 全部通過，80% coverage。

  交付物

  ┌─────────────────────────┬──────────────────────────────────────────────────────────────────────────┐
  │          模組           │                                   說明                                   │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ bbdsl/core/expander.py  │ SUIT_META (4花色×10屬性)、foreach_suit 遞迴展開（最深2層）、變數替換引擎 │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ bbdsl/core/validator.py │ 10 條驗證規則（8 實作 + 2 stub）、ValidationResult/Report 模型           │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ bbdsl/cli/main.py       │ 新增 expand 和 validate 子命令                                           │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ 8 個測試 fixture        │ 6 invalid + 2 valid YAML 檔                                              │
  ├─────────────────────────┼──────────────────────────────────────────────────────────────────────────┤
  │ 38 個新測試             │ 18 expander + 20 validator                                               │
  └─────────────────────────┴──────────────────────────────────────────────────────────────────────────┘

  CLI 可用命令

  bbdsl expand <path>              # 展開 foreach_suit
  bbdsl validate <path>            # 執行 10 條規則，exit code: 0=pass, 1=warning, 2=error
  bbdsl validate <path> --rules val-002,val-008   # 只跑指定規則

  val-002 設計決策

  val-002 (no-overlap) 採保守策略：僅標記兩個同層叫品在 HCP 和 相同花色長度都重疊的情況。跳過 artificial 叫品和 shape
  ref，因為這些在真實系統中透過優先順序區分（完整的 priority-based 重疊檢測將在 Phase 2 的 val-014 實作）。

  ---

Sprint 1.3 完成總結

  交付物

  ┌─────────────────────────────────┬──────────────────────────────────────────────────────────┐
  │              模組               │                           說明                           │
  ├─────────────────────────────────┼──────────────────────────────────────────────────────────┤
  │ bbdsl/importers/bml_importer.py │ BML 解析器 + 語義提取引擎 + BBDSL 轉換器                 │
  ├─────────────────────────────────┼──────────────────────────────────────────────────────────┤
  │ 3 個 BML test fixtures          │ simple_precision.bml, sayc_opening.bml, unresolvable.bml │
  ├─────────────────────────────────┼──────────────────────────────────────────────────────────┤
  │ 40 個新測試                     │ tests/test_importers/test_bml_importer.py                │
  ├─────────────────────────────────┼──────────────────────────────────────────────────────────┤
  │ CLI bbdsl import bml            │ 完整的 BML 匯入子命令，含彩色輸出和 exit code            │
  └─────────────────────────────────┴──────────────────────────────────────────────────────────┘

  CLI 四個命令全部可用

  bbdsl load precision.bbdsl.yaml         # Sprint 1.1 ✓
  bbdsl expand precision.bbdsl.yaml       # Sprint 1.2 ✓
  bbdsl validate precision.bbdsl.yaml     # Sprint 1.2 ✓
  bbdsl import bml sayc_opening.bml       # Sprint 1.3 ✓

  BML 語義提取能力

  - HCP range (12-15 HCP) 和 min-only (16+ HCP)
  - 花色長度（5+ hearts、4+h、6♥）
  - 牌型關鍵字（bal → balanced、semi-bal → semi_balanced）
  - Forcing 等級（GF / f1 / inv / NF / sign-off 等 14 個關鍵字）
  - 人工/Alert 標記（自動推斷：artificial → alertable=true）
  - 先制（preemptive、weak）

  UnresolvedNode (ADR-4)

  無法提取手牌約束的描述 → is_unresolved: true + bml_original + reason，輸出 YAML 含 WARNING 提示，exit code=1。

  Phase 1 驗收標準達成

  ┌───────────────────┬────────┐
  │       項目        │  狀態  │
  ├───────────────────┼────────┤
  │ bbdsl load 精準制 │ ✅     │
  ├───────────────────┼────────┤
  │ foreach_suit 展開 │ ✅     │
  ├───────────────────┼────────┤
  │ 8 條驗證規則      │ ✅     │
  ├───────────────────┼────────┤
  │ BML 匯入          │ ✅     │
  ├───────────────────┼────────┤
  │ CLI 四個命令      │ ✅     │
  ├───────────────────┼────────┤
  │ 測試覆蓋率 > 80%  │ ✅ 82% │
  └───────────────────┴────────┘

---

● Sprint 2.1 完成總結

  188 tests passed, 81% coverage.

  交付物

  ┌──────────────────────────────────────┬───────────────────────────────────────────────────────────────────────────┐
  │                 模組                 │                                   說明                                    │
  ├──────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ bbdsl/exporters/bboalert_exporter.py │ 叫牌樹扁平化 → context,call,explanation                                   │
  │                                      │ CSV；手牌約束轉自然語言；Alert/GF/F1/S/O 旗標；zh-TW 支援                 │
  ├──────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ bbdsl/importers/bboalert_importer.py │ CSV 解析 → 叫牌樹重建；複用 BML 語義提取器；UnresolvedNode (ADR-4)        │
  ├──────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 1 個 BBOalert fixture                │ tests/fixtures/bboalert_samples/simple_precision.bboalert                 │
  ├──────────────────────────────────────┼───────────────────────────────────────────────────────────────────────────┤
  │ 47 個新測試                          │ test_bboalert_exporter.py (28) + test_bboalert_importer.py (19，含        │
  │                                      │ round-trip)                                                               │
  └──────────────────────────────────────┴───────────────────────────────────────────────────────────────────────────┘

  新 CLI 命令

  # Export
  bbdsl export bboalert precision.bbdsl.yaml -o precision.bboalert
  bbdsl export bboalert precision.bbdsl.yaml --locale zh-TW  # 中文說明

  # Import
  bbdsl import bboalert precision.bboalert -n "Precision" -o out.bbdsl.yaml

  Round-trip 行為

  - Export (lossless)：BBDSL → BBOalert 保留所有叫品和說明
  - Import (lossy)：BBOalert 文字說明有 HCP/花色資料的可自動提取；純文字描述（如 "Stayman"）→ is_unresolved:
  true，需手動補完

  BBOalert 格式

  # context,call,explanation
  ,1C,"16+ HCP, artificial strong club; [Alert, F1]"
  1C,1D,"0-7 HCP, artificial negative; [Alert]"
  1C,1H,"8+ HCP, 5+ hearts; [GF]"
  1NT,2C,"Stayman; [Alert]"

  ---

  ● Sprint 2.2 is complete. Here's a summary of what was implemented:

  Sprint 2.2 完成 ✅

  新增內容

  bbdsl/core/validator.py — 新增 2 條規則：
  - val-013 priority-unique: 偵測同層兄弟節點中重複的 priority 值
  - val-014 selection-rules-exhaustive: 檢查 selection_rules 必須含有 condition: "true" 的 catch-all 規則
  - 驗證規則總數：12 條（10 real + 2 stubs）

  新增 fixture 檔案：
  - tests/fixtures/valid/with-selection-rules.yaml — 含 selection_rules 的合規文件
  - tests/fixtures/invalid/priority-duplicate.yaml — val-013 違規
  - tests/fixtures/invalid/no-catchall-rules.yaml — val-014 違規

  tests/test_core/test_selector.py — 57
  個測試（evaluate_condition、parse_selection_rules、select_bid、select_opening、hand_from_constraint）

  tests/test_exporters/test_bml_exporter.py — 29 個測試（_build_bml_description、export_bml）

  bbdsl/cli/main.py — 新增 2 個命令：
  - bbdsl export bml — 匯出 BML 格式（支援 --suit-symbols、--locale）
  - bbdsl select — 用 selection_rules 選擇叫品（支援 --hcp、--hearts 等手牌參數）

  測試結果：254 tests passed, 80% coverage

  ---

  ● Sprint 2.3 完成。以下是本次的總結：

  Sprint 2.3 完成 ✅

  新增範例制度

  examples/sayc.bbdsl.yaml — Standard American Yellow Card
  - 14 個開叫（1♣/1♦/1♥/1♠/1NT/2♣/2♦/2♥/2♠/2NT/3♣/3♦/3♥/3♠）
  - Stayman + Jacoby Transfer conventions
  - 全部 14 條驗證規則通過（14/14 passed）

  examples/two_over_one.bbdsl.yaml — 2/1 Game Forcing
  - 9 個開叫 + 完整的 2/1 GF 回應系統
  - Forcing 1NT convention（bbdsl/forcing-nt-v1）
  - 全部 14 條驗證規則通過（14/14 passed）

  examples/precision.bbdsl.yaml — 補完 2H/2S 回應 → 現在也是 14/14

  升級驗證器（val-001/003/009/010）

  - val-001 hcp-coverage：實際檢查 HCP 0-37 覆蓋缺口，警告 10+ HCP 的空白
  - val-003 response-complete：每個開叫必須至少有一個回應
  - val-009 seat-vul-no-conflict：偵測 context_overrides 中重複的座位/身價組合
  - val-010 foreach-expansion-conflict：展開後檢查兄弟節點重複叫品

  整合測試

  - 3 個制度 × 14 條規則的參數化整合測試
  - 新增 fixture：seat-vul-conflict.yaml、foreach-conflict.yaml

  測試結果：277 tests passed, 81% coverage

  ---
● Sprint 3.1 is complete. Here's the summary:

  Sprint 3.1 完成

  交付物

  ┌────────────────────────────────────────────┬─────────────────────────────────┐
  │                    檔案                    │              狀態               │
  ├────────────────────────────────────────────┼─────────────────────────────────┤
  │ bbdsl/exporters/html_exporter.py           │ ✅ 新增 (已在上個 context 建立) │
  ├────────────────────────────────────────────┼─────────────────────────────────┤
  │ bbdsl/cli/main.py                          │ ✅ 新增 export html 子命令      │
  ├────────────────────────────────────────────┼─────────────────────────────────┤
  │ tests/test_exporters/test_html_exporter.py │ ✅ 新增 57 個測試               │
  └────────────────────────────────────────────┴─────────────────────────────────┘

  功能

  # 輸出到標準輸出
  uv run bbdsl export html examples/precision.bbdsl.yaml

  # 輸出到檔案
  uv run bbdsl export html examples/precision.bbdsl.yaml -o viewer.html

  # 繁中介面 + 花色符號
  uv run bbdsl export html examples/precision.bbdsl.yaml -o viewer.html --locale zh-TW --suit-symbols

  # 自訂標題
  uv run bbdsl export html examples/precision.bbdsl.yaml --title "精準制叫牌系統"

  HTML Viewer 特性

  - Tailwind CSS CDN — 無需 build step
  - 開叫=藍色、回應=綠色 — 顏色區分角色
  - 可摺疊叫牌樹 — 預設只顯示開叫層
  - Badges: art(紫)、GF/F1(橙)、pre(紅)
  - 搜尋高亮 — 即時過濾叫品路徑
  - Hover tooltip — 手牌限制說明
  - 特約模組區塊 — 可折疊
  - 完整度指示條 (Completeness bar)

  測試結果

  334 passed, 81% coverage
  html_exporter.py: 97% coverage

---

● Sprint 3.2 complete. Summary:

  ---
  Sprint 3.2 完成

  交付物

  ┌────────────────────────────────────────────────┬──────────────────────────────────────┐
  │                      檔案                      │                 狀態                 │
  ├────────────────────────────────────────────────┼──────────────────────────────────────┤
  │ bbdsl/exporters/convcard_exporter.py           │ ✅ 新增                              │
  ├────────────────────────────────────────────────┼──────────────────────────────────────┤
  │ bbdsl/exporters/svg_tree.py                    │ ✅ 新增                              │
  ├────────────────────────────────────────────────┼──────────────────────────────────────┤
  │ bbdsl/cli/main.py                              │ ✅ 新增 export convcard + export svg │
  ├────────────────────────────────────────────────┼──────────────────────────────────────┤
  │ tests/test_exporters/test_convcard_exporter.py │ ✅ 新增 58 個測試                    │
  ├────────────────────────────────────────────────┼──────────────────────────────────────┤
  │ tests/test_exporters/test_svg_tree.py          │ ✅ 新增 48 個測試                    │
  └────────────────────────────────────────────────┴──────────────────────────────────────┘

  CLI 用法

  # Convention Card (WBF/ACBL)
  uv run bbdsl export convcard examples/precision.bbdsl.yaml -o precision.html
  uv run bbdsl export convcard examples/sayc.bbdsl.yaml --style acbl --locale zh-TW

  # SVG 叫牌樹
  uv run bbdsl export svg examples/precision.bbdsl.yaml -o tree.svg --max-depth 1
  uv run bbdsl export svg examples/sayc.bbdsl.yaml --suit-symbols --max-depth 2

  主要功能

  Convention Card:
  - Opening Bids 表格（Bid | HCP | Description | Flags）
  - 1NT Section（HCP range、Stayman ✓/✗、Jacoby Transfer ✓/✗）
  - Strong 2C Section（if present）
  - Weak Two Section（if present）
  - Conventions list（ID → 名稱 → 描述）
  - Completeness 狀態 badges
  - WBF / ACBL style 切換
  - @media print CSS → 可直接列印為 PDF

  SVG 叫牌樹:
  - 自動 top-down 樹狀佈局（Reingold-Tilford 簡化版）
  - max_depth 可配置（0=only openings, 1=+responses, 2=+continuations）
  - opener=藍色節點、responder=綠色節點
  - Cubic bezier 連線
  - 可嵌入 HTML

  測試結果

  440 passed, 81% coverage
  convcard_exporter.py: 94%
  svg_tree.py: 86%

  ---

● Sprint 3.3 is complete. Here's the summary:

  ---
  Sprint 3.3 完成 ✅

  584 tests pass, 82% coverage

  新增檔案

  ┌────────────────────────────────────────────┬────────────────────────────────────────────────────┐
  │                    檔案                    │                        說明                        │
  ├────────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ bbdsl/core/hand_generator.py               │ BridgeHand dataclass + 兩階段拒絕取樣手牌產生器    │
  ├────────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ bbdsl/core/quiz_generator.py               │ QuizQuestion dataclass + 開叫/回應題型產生器       │
  ├────────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ bbdsl/exporters/quiz_exporter.py           │ 互動式 HTML 測驗匯出器（教學模式、得分板、進度條） │
  ├────────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ tests/test_core/test_hand_generator.py     │ 31 tests                                           │
  ├────────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ tests/test_core/test_quiz_generator.py     │ 57 tests                                           │
  ├────────────────────────────────────────────┼────────────────────────────────────────────────────┤
  │ tests/test_exporters/test_quiz_exporter.py │ 56 tests                                           │
  └────────────────────────────────────────────┴────────────────────────────────────────────────────┘

  修改檔案

  - bbdsl/cli/main.py — 新增 bbdsl quiz 頂層命令

  使用方式

  uv run bbdsl quiz examples/precision.bbdsl.yaml -o quiz.html --locale zh-TW -n 20

  ---

  ● Sprint 4.1 完成 ✅

  ---
  成果摘要

  697 tests pass, 82% coverage

  新增檔案

  ┌─────────────────────────────────────────────┬─────────────────────────────────────────────────────┐
  │                    檔案                     │                        說明                         │
  ├─────────────────────────────────────────────┼─────────────────────────────────────────────────────┤
  │ bbdsl/exporters/ai_kb_exporter.py           │ AI 知識庫匯出器：DFS 扁平化叫牌樹 → JSON/JSONL，含  │
  │                                             │ context_text RAG 文字                               │
  ├─────────────────────────────────────────────┼─────────────────────────────────────────────────────┤
  │ bbdsl/core/dealer_bridge.py                 │ Dealer 橋接器：HandConstraint ↔ Dealer              │
  │                                             │ 條件字串雙向轉換                                    │
  ├─────────────────────────────────────────────┼─────────────────────────────────────────────────────┤
  │ tests/test_exporters/test_ai_kb_exporter.py │ 57 tests                                            │
  ├─────────────────────────────────────────────┼─────────────────────────────────────────────────────┤
  │ tests/test_core/test_dealer_bridge.py       │ 56 tests                                            │
  └─────────────────────────────────────────────┴─────────────────────────────────────────────────────┘

  修改檔案

  - bbdsl/cli/main.py — 新增 export ai-kb 和 export dealer 命令

  使用範例

  # AI KB（每條叫牌規則 + context_text 供 RAG 使用）
  uv run bbdsl export ai-kb examples/precision.bbdsl.yaml -o precision.jsonl --format jsonl

  # Dealer 腳本（各開叫的手牌條件）
  uv run bbdsl export dealer examples/precision.bbdsl.yaml

---


● Sprint 4.2 已實作完成。

摘要

Sprint 4.2 — 模擬對練引擎實作完成：

新增檔案

- **sim_engine.py** — 核心模擬引擎  
  - `Deal` dataclass：從 52 張牌的牌堆發出的 4 手牌  
  - `AuctionStep` dataclass：一次叫牌步驟（seat/bid/by/reasoning）  
  - `SimulationResult` dataclass：單次發牌的整體結果  
  - `generate_deal(seed)` — 洗牌後從 52 張牌平均分配 4×13  
  - `_matches_constraint()` — 重用 `hand_generator` 的 `_check_hcp`/`suit`/`shape`/`controls`  
  - `_navigate_tree()` — 透過 `ns_path` 瀏覽叫牌樹（openings → responses → continuations 交替）  
  - `simulate_deal()` — 單局完整叫牌模擬  
  - `simulate()` — 批次模擬 n 局  
- **test_sim_engine.py** — 74 個測試

修改檔案

- **main.py** — 新增 `bbdsl simulate` 指令

測試結果

- 新測試：74/74 通過  
- 全部：771/771 通過（先前 697 → 新增 74）  
- 覆蓋率：保持 82%

---

 Sprint 4.3 完成總結 — 制度比較器 + PBN 匯出

  新增檔案

  bbdsl/core/comparator.py — 制度比較引擎
  - DiffCase dataclass：記錄兩套制度叫牌結果不同的單筆牌例（包含雙方叫牌序列與最終合約）
  - ComparisonReport dataclass：彙整統計數據，提供 agree_rate 一致率屬性與 summary_text()（支援 zh-TW）
  - compare_systems(doc_a, doc_b, n_deals, seed) — 對相同隨機牌例分別跑兩套制度，回傳差異報告

  bbdsl/exporters/pbn_exporter.py — PBN 格式匯出器
  - _hand_to_pbn() — 將 BridgeHand 轉為 "AK73.A86.AQ7.K73" 格式，空門牌型以 "-" 表示
  - _deal_to_pbn_deal() — 從莊家位起順時針排列四手牌
  - _auction_to_pbn() — 每行 4 個叫品的標準 PBN 叫牌格式
  - _build_note() — 在 [Note] 標籤中嵌入 BBDSL 語義（制度名稱 + N/S 叫牌推理）
  - export_pbn(doc, n_deals, seed, ...) — 模擬後匯出完整 PBN 牌譜文字

  修改檔案

  bbdsl/cli/main.py — 新增兩個 CLI 命令：
  uv run bbdsl compare precision.yaml sayc.yaml -n 50 --seed 42
  uv run bbdsl export pbn precision.yaml -n 10 --seed 42 -o out.pbn

  測試結果

  ┌────────────┬──────────────────────────────────────────────────────────┐
  │    項目    │                           數值                           │
  ├────────────┼──────────────────────────────────────────────────────────┤
  │ 新增測試   │ 80 個（test_comparator.py 35 + test_pbn_exporter.py 45） │
  ├────────────┼──────────────────────────────────────────────────────────┤
  │ 全部測試   │ 851 / 851 通過                                           │
  ├────────────┼──────────────────────────────────────────────────────────┤
  │ 測試覆蓋率 │ 82%                                                      │
  └────────────┴──────────────────────────────────────────────────────────┘

---

● CLAUDE.md 已更新完成。主要變更如下：

  更新內容摘要：

  ┌──────────────┬─────────────────────────────────┬──────────────────────────────────────────────────┐
  │     項目     │          舊版（過時）           │                   新版（現況）                   │
  ├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 狀態         │ Phase 1 complete, 141 tests     │ Phase 4.3 complete, 851 tests                    │
  ├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 套件結構     │ 只有 core/、models/、importers/ │ 完整 8 個模組（含                                │
  │              │                                 │ sim_engine、comparator、pbn_exporter 等）        │
  ├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
  │ CLI 命令     │ 6 個命令                        │ 20+ 個命令（含 simulate、compare、export pbn     │
  │              │                                 │ 等）                                             │
  ├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 架構說明     │ 提到「Phase 2+」、「Phase       │ 全部已實作，移除未來式描述                       │
  │              │ 3+」為未來                      │                                                  │
  ├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 範例檔案     │ 只提 sayc.bbdsl.yaml（process/  │ precision + sayc + two_over_one（examples/ 下）  │
  │              │ 下）                            │                                                  │
  ├──────────────┼─────────────────────────────────┼──────────────────────────────────────────────────┤
  │ 實作注意事項 │ 無                              │ 新增 Deal 發牌、拍賣終止條件、PBN Note           │
  │              │                                 │ 格式、Fixtures 陷阱等                            │
  └──────────────┴─────────────────────────────────┴──────────────────────────────────────────────────┘

---



